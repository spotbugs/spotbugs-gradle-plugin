apply plugin: "jacoco"

jacoco {
  toolVersion = "0.8.1"
}

jacocoTestReport {
  reports {
    html.enabled = true
  }
}

test {
  jacoco {
    enabled = System.getProperty("java.specification.version") != "9"
    append = true
    includeNoLocationClasses = true
  }
}
